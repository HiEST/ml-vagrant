Vagrant Box fot testing ML features
==================================

##Steps:
1. run 'vagrant up **provision**' 
2. run 'vagrant ssh' to log into the machine
3. run 'vagrant halt' to stop the VM
4. run 'vagrant destroy' to delete the VM and its associated image


